# Thin eval pointer. The investigation reads the SHARED corpus under ../../telemetry/,
# sliced to this window/as_of. The answer key is HIDDEN_TRUTH.md (never fed to the agent).
id: A
slug: checkout-504
workspace: acme-checkout
query: "checkout is throwing 504s and customers are complaining"
mode: live                       # paged mid-incident; investigate as-of `now`, looking back
as_of: 2026-06-16T15:15:00Z      # engine clamps visible telemetry to <= as_of (no hindsight).
                                 # The war-room conclusion isn't reached until later — the agent must derive it.
look_back: 2h                    # suggested ~13:15–15:15; agent may widen
severity: SEV1
evidence: telemetry/             # logs, metrics, alerts, deploys, chat, captures — all shared
